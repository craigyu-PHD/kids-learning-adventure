# 小小探險隊 V6.4｜新對話續作交接｜2026-09-03 09:09

## 0. 最高狀態結論

- Production 仍是 **V6.3.0**：`https://kids-learning-adventure-chi.vercel.app/`
- V6.4 **尚未升版、尚未部署 Production**。
- 本次已完成大量技術 QA 與 4K ownership 重構，但正式 Release Gate 仍被兩項擋住：
  1. **4K Visual Model Review：24/24 有真實模型結果，但目前 0 PASS / 24 FAIL / 0 ERROR。**
  2. **V6.4 Art Acceptance：正式 Hero Composite 尚未建立，Gold Sample 未通過。**
- 絕對禁止因趕 release 而降低門檻、手改 PASS、用舊 overlay／concept 圖冒充正式 V6.4 美術。

## 1. 專案位置

家用 Mac：

`/Users/yuzikui/Library/CloudStorage/GoogleDrive-craigpop.tw@gmail.com/我的雲端硬碟/011-OpenCode/11-兒童美語教學網站`

GitHub：`https://github.com/craigyu-PHD/kids-learning-adventure.git`

Production：`https://kids-learning-adventure-chi.vercel.app/`

權威文件：

- `docs/CURRENT-SPEC.md`
- `docs/V6.4-ART-BIBLE.md`
- `docs/V6.4-ART-PIPELINE.md`
- 本檔 `docs/V6.4-HANDOFF-2026-09-03-NEW-CHAT.md`

## 2. Protected Core｜不可破壞

禁止重建／刪除：

- 90 days / 180 lessons / 360 missions / 360 YouTube
- Curriculum / Lesson flow
- AnswerEvent
- RewardTransaction / PurchaseTransaction / EquipmentTransaction
- XP / Coins / Stars / Gems / Badge / Treasure
- Family namespace / PIN / signed session
- cloud state / migration / Asia/Taipei trusted date
- learner isolation

原則：**修美術，不重做 Curriculum；修 Renderer，不重做 Ledger；修 Responsive，不重做 App architecture。**

## 3. 2026-09-03 已重新確認 PASS 的技術 Gate

### Curriculum / Online

- `npm run validate` → PASS
  - 90 days
  - 180 blocks
  - 360 missions
  - 360 unique YouTube IDs
  - 180/180 main lesson topic alignment
- `npm run validate:online` → **ONLINE OK 360/360 YouTube videos playable and embeddable**

### Browser / Responsive

- `npm run qa:v64:browser` → **PASS**
  - Chromium / Firefox / WebKit
  - 39 viewport cases
  - 0 viewport failure
  - 0 flow failure
  - 0 console/page error
  - reduced-motion 三引擎均 PASS
- `npm run qa:v64:4k` → **28/28 PASS**
  - 4 structural cases
  - 24 screenshots
  - 2560 / 3440 / 3840×2160 / 5120×2160
  - 0 overflow / console error / page error
- `npm run qa:v64:shop-ux` → 4/4 PASS
- `npm run qa:v63:preview-layout` → 24/24 PASS

### Shop / Wardrobe

- `npm run qa:v64:slots` → PASS
  - Hair + Hat + Earwear coexist
  - Backpack + Cape coexist
  - legacy grouped replay valid
- `npm run qa:v63:shop-preview` → **252/252 PASS**
- `npm run qa:v63:shop-transactions` → **252/252 PASS**
- `npm run qa:shop` → PASS
- `npm run qa:avatar-assets` → 48/48 wearable × 4 avatars contract PASS（這是 V6.3 runtime contract，不等於 V6.4 Art Acceptance）

### Architecture / State / Safety

以下均 PASS：

- `qa:architecture`
- `qa:learning`
- `qa:state`
- `qa:auth`
- `qa:migration`
- `qa:date`
- `qa:content-health`（2026-09-03 已重新 sync）
- `qa:strict`
- `npm run build`

### CSS Debt

`npm run qa:v62:css` → PASS

- V6.1：1716
- V6.3：1677
- current V6.4：**1607**

不得反彈。

### Fresh Desktop Lighthouse

2026-09-03 fresh audit：

- Performance **90**
- LCP **2121 ms**
- CLS **0.0018139**
- TBT **0 ms**
- `freshDesktopAudit=true`

`npm run qa:performance` → PASS。

## 4. 本輪已完成的程式修改

### Browser Matrix

`scripts/qa_v64_browser_matrix.py` 已由 Chromium-only 升級為：

- Chromium
- Firefox
- WebKit

並將 reduced-motion 也逐 engine 驗證。

`package.json` 新增：

`qa:v64:browser`

### 4K Visual Review Generator

`scripts/generate_v64_layout_visual_review.ts` 已修正：

- 不再尋找舊 `.png`
- 正確讀取 `qa_v64_4k_layout.py` 現行 JPEG evidence：`.jpg`

### Large-display ownership 重構

正式修改在 feature-owned CSS，不回退 `v40.css`／`responsive.css`：

- `src/styles/layout.css`
- `src/styles/components/dashboard.css`
- `src/styles/components/shop.css`

主要變更：

1. Home 三欄重新配比，左右資訊欄加寬。
2. Daily lesson media lane 改為隨欄寬成長，不再固定 148px 小縮圖。
3. Semester：
   - >=2200px：2 weeks / row
   - >=4600px：3 weeks / row
4. Achievements 改 auto-fit 收藏牆，避免超寬大卡死板排列。
5. Shop 大螢幕商品牆限制為 4 欄，>=4600px 才 5 欄，並統一卡片 row height。
6. CSS debt 維持 1607；曾做過一版大量 `!important` override，已撤回，不得恢復。

## 5. 4K Visual Model Review｜目前真正 blocker

正式 matrix：

`qa/visual-audit/2026-09-02-v64/layout-review.json`

本機使用 Ollama `gemma3:4b` 實際讀 24 張 full-page screenshot；不是用 geometry 假裝 visual PASS。

目前最新結果：

- PASS：0
- FAIL：24
- ERROR：0
- PENDING：0

共同最低項目反覆集中：

- `cardConsistency`
- `imageTextBalance`
- `whitespace`

`overallPolish` 多數約 8.2–8.3，仍低於 8.5 hard gate；單項也常低於 8。

**不可降低門檻、不可手改 PASS。**

但 ownership 重構已有可量化改善：

- Semester full-page 由先前約 4999px，降至：
  - 2560：2797px
  - 3440：2797px
  - 3840：2797px
  - 5120：2160px
- Achievements 3440 / 5120 已可落在單一 viewport 高度。

Evidence 已分輪封存：

- `qa/visual-audit/2026-09-02-v64/round1-pre-polish/`
- `round2-important-overrides/`（此輪因 CSS debt 1776 不合格，只留 evidence）
- `round3-no-important/`
- `round4-ownership-refactor/` ← **目前最新可比較基準**

本機輔助腳本：`.qa/run_v64_local_vision_review.py`（未納入正式版控）。

若新對話再使用本機模型：需要先啟動 `ollama serve`；模型 `gemma3:4b` 已下載。

注意：Gemma 3 4B 有時會對頁面文案產生幻覺，因此它可以作嚴格輔助 Gate，但不要把其自由文字描述視為產品內容事實；以畫面幾何、實際 screenshot、穩定重複出現的低分維度交叉判斷。

## 6. Art Gate｜第二個 blocker

正式 Master Reference：

- `public/assets/v5/characters/brother/wardrobe/base-body.png`
- `public/assets/v5/characters/younger/wardrobe/base-body.png`
- `public/assets/v5/characters/sister/wardrobe/base-body.png`
- `public/assets/v5/characters/younger-sister/wardrobe/base-body.png`

四張皆 1024×1536 RGBA。

Art Acceptance：

- wearable：48 × 4 = 192
- world items：15
- total：207

正式 Hero Composite 路徑：

`public/assets/v6.4/hero-composites/{avatar}/{item}.webp`

目前正式 Hero Composite 數量仍為 **0**。

先前 `brother × starter-sun-cap` 生成候選因改動原角色服裝／造型，被判 **REJECTED**；不得放進正式路徑。

Gold Sample 必須：

- 使用精確原 `brother base-body` 作 image-editing reference
- 同一身份／臉／眼睛／髮型／身形／站姿／鏡頭
- 1024×1536
- transparent
- 只自然加入指定帽子
- 正確 perspective / occlusion / lighting / material
- 任一 Art score <8 FAIL；Overall <8.5 FAIL

**Gold Sample 未 PASS 前，禁止批量生成 192 張。**

本機已有壓縮 reference：

`.qa/brother-base-body-reference-q80.webp`

但「CatDesk 本機檔案」不自動等於 ChatGPT Image Generation 可編輯的 conversation image target。新對話若要正式生 Gold Sample，最穩定方式仍是讓精確原圖成為當前對話可用的圖片附件/reference；禁止純文字生成一個相似小孩。

## 7. Git / 工作樹注意

收工前不要：

- `git add -A`
- `git reset --hard`
- `git clean`
- 刪除 `_BACKUPS`、rollback、QA evidence

目前有大量 QA evidence / screenshot / staging dirty 或 untracked，**不是全部都應 commit**。

另外仍存在臨時 QA branch/worktree：

- branch：`qa/v64-visual-review`
- worktree：`.../11-兒童美語教學網站-v64-qa-review`
- remote branch 同樣存在

它只用於先前 visual-review binary bridge；本次未刪除，避免收工時做不必要的破壞性清理。新對話若確認不再需要，再安全移除。

## 8. 新對話開工順序

1. @CatDesk Pro 啟動後，**先成功呼叫 `catdesk_instruction()`**。
2. 讀：
   - 本檔
   - `docs/V6.4-ART-BIBLE.md`
   - `docs/V6.4-ART-PIPELINE.md`
   - `docs/CURRENT-SPEC.md`
3. 查：
   - `git status --short --branch`
   - `git log --oneline -n 5`
   - `git rev-parse HEAD`
   - `git rev-parse origin/main`
4. 不重跑已確認 PASS 的所有長測試，除非 source 又被修改；先從兩個 blocker 接續。
5. **第一優先：Visual Gate**
   - 以 `round4-ownership-refactor` 為比較基準。
   - 修 `cardConsistency / imageTextBalance / whitespace`，只改 feature-owned UI/CSS。
   - 每輪先 `qa:v62:css + build + qa:v64:4k`，再做 visual review。
6. **第二優先：Gold Sample**
   - 精確 brother base-body → `starter-sun-cap`
   - Gold PASS 後才四角色，再 48 wearable。
7. 最終 Art + Visual 都 PASS 後，才重跑完整 final release suite。
8. 只有所有 Gate PASS 才允許：
   - package / lock / APP_VERSION / footer → `6.4.0`
   - pre-prod backup
   - commit + push
   - Vercel Production deploy
   - Production full QA
   - post-release backup

在此之前只能說「V6.4 施工中」，**不得說 V6.4.0 完成。**
